class Shape {
  fields color: string;

  def setColor(color: int) = {
    self.color := color;
  }

  def getColor(): string = {
    return self.color;
  }

  def draw() = {} 
}

class Square extends Shape {
  def Square() = {
    self.color := "Hello World!";
    draw();
  }
}
 
