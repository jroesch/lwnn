class Shape {
  fields color: string;

  methods
  def setColor(color: int) = {
    self.color := color;
  }

  def getColor(): string = {
    return self.color;
  }

  def draw() = {} 
}

class Square extends Shape {
  methods
  def Square() = {
    self.color := "Hello World!";
    self.draw();
  }
}
 
