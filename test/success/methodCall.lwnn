class Add {
  def add_two(x: int, y: int, z: int): int = {
    z := x + y;
    return x + y;
  }
}

class MethodCall {
  def MethodCall(add: Add, temp: int) = {
    add := new Add();
    temp := add.add_two(10, 1);
  }
}
   



